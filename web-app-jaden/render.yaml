services:
  - type: web
    name: moodflix-api
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn api_server_simple:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
      - key: FLASK_ENV
        value: production
